# Optional canonical URL used by metadata, robots, and sitemap.
SITE_URL=http://localhost:3000

# Optional external URL for Playwright instead of its managed local server.
# PLAYWRIGHT_BASE_URL=https://example.com
